# Write a program that will calculate the number of trailing zeros in
# a factorial of a given number.

def zeros(n):
    c = 0
    while n//5 > 0:
        c += (n//5)
        n = n//5
    return c
    
    
    
    
print(zeros(0))   # 0
print(zeros(6))   # 1
print(zeros(30))  # 7   
