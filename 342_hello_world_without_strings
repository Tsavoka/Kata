# Create the hi_all() function without using strings, numbers and
# booleans. The return value is "Hello World". No, it is not impossible,
# use the builtin functions. Good luck :)

from string import whitespace


def hi_all():
    return Hello.__name__ + next(iter(whitespace)) + World.__name__

def Hello(self):
    pass

def World(self):
    pass




print(hi_all()) # "Hello World"
